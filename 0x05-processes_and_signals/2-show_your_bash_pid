#!/usr/bin/env bash
# Find and display all running processes that contain "bash" in their name.
# Use the "ps" command to list all running processes, then pipe the output to "grep".
ps aux | grep bash
